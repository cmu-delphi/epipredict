# Extract the frosting object from a workflow

Extract the frosting object from a workflow

## Usage

``` r
extract_frosting(x, ...)
```

## Arguments

- x:

  an `epi_workflow` object

- ...:

  not used

## Value

a `frosting` object
