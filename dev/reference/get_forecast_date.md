# Extract the as_of for the forecast date, and make sure there's nothing very off about it.

Extract the as_of for the forecast date, and make sure there's nothing
very off about it.

## Usage

``` r
get_forecast_date(new_data, info, epi_keys_checked, latency, columns = NULL)
```
