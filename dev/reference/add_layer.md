# Add layer to a frosting object

Add layer to a frosting object

## Usage

``` r
add_layer(frosting, object)
```

## Arguments

- frosting:

  a `frosting` postprocessor

- object:

  a `frosting` layer

## Value

an updated `frosting` postprocessor
