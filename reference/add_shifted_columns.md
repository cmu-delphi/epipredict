# backend for both `bake.step_epi_ahead` and `bake.step_epi_lag`, performs the checks missing in `epi_shift_single`

backend for both `bake.step_epi_ahead` and `bake.step_epi_lag`, performs
the checks missing in `epi_shift_single`

## Usage

``` r
add_shifted_columns(new_data, object)
```
